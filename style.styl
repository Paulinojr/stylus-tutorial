@import 'styl/*'

body
    background primary
    color: red
    border: solid 1px @color
    main
        background shade(primary, 50%) 
        color primary
        border solid 1px @color
        border-radius 10px
        clear-fix()